<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_sale_order_form_followup" model="ir.ui.view">
        <field name="name">sale.order.form.optical.followup</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_open_followup_schedule" type="object"
                        class="oe_stat_button" icon="fa-calendar-check-o"
                        invisible="not followup_schedule_id"
                        groups="optical.group_optical_user">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Fidélisation</span>
                    </div>
                </button>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="Fidélisation" name="optical_followup" groups="optical.group_optical_user">
                    <group>
                        <field name="delivered_date" readonly="1"/>
                        <field name="followup_schedule_id" readonly="1"/>
                        <field name="picking_late_alert_sent" readonly="1"
                               invisible="not picking_late_alert_sent"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

</odoo>
