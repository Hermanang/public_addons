<?xml version="1.0" encoding="utf-8" ?>
<!--
    Copyright 2017 LasLabs Inc.
    License LGPL-3 or later (http://www.gnu.org/licenses/lgpl.html).
-->
<odoo>

    <record model="ir.ui.view" id="account_invoice_form">
        <field name="name">Account Invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='invoice_line_ids']/list/field[@name='product_id']" position="before">
                <field name="subscriber" optional="hide"/>
                <field name="beneficiary_id" optional="hide"/>
                <field name="pec_policy" optional="hide"/>
            </xpath>
        </field>
    </record>

    <!-- Vue pour afficher les champs optiques et bouton de recalcul (F10 fix) -->
    <record model="ir.ui.view" id="account_invoice_form_optical_relations">
        <field name="name">Account Invoice - Optical Relations</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@id='other_tab']" position="inside">
                <group string="Relations Optiques" name="optical_relations">
                    <group>
                        <field name="optical_subscriber_id" readonly="1"/>
                        <field name="optical_insurance_id" readonly="1"/>
                    </group>
                    <group>
                        <field name="optical_prescriber_id" readonly="1"/>
                        <field name="optical_clinic_id" readonly="1"/>
                    </group>
                    <button name="action_recompute_optical_relations"
                            string="Recalculer les relations"
                            type="object"
                            class="btn-secondary"
                            groups="base.group_user"
                            invisible="state != 'posted'"/>
                </group>
            </xpath>
        </field>
    </record>


</odoo>
